import CreateProjectForm from '@/components/projects/CreateProjectForm';

export const metadata = {
  title: 'New Project — AI-Constructed',
};

export default function CreateProjectPage() {
  return <CreateProjectForm />;
}
